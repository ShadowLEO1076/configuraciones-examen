server:
  port: 8085 # El puerto que le quieras asignar a este microservicio

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # Ajusta si tu Eureka usa otro puerto
